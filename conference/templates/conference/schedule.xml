<?xml version="1.0" encoding="utf-8" ?>
{% spaceless %}
{% load conference %}
<schedule conference="{{ schedule.conference }}" id="{{ schedule.slug }}" time_slot="15">
    {% conference_schedule schedule.conference schedule.slug as tt %}
    {% for time, events in tt %}
        {% if time %}
        <slot time="{{ time }}"{% if event.class %} class="{{ events.class|join:"," }}"{% endif %}>
            {% for event in events.events %}
                {% if event %}
                <event tracks="{{ event.track_slots }}" tags="{{ event.tags|join:"," }}" duration="{{ event.time_slots }}" title="{{ event.title|striptags }}">
                {% if event.sponsor %}
                <sponsor name="{{ event.sponsor }}" url="{{ event.sponsor.url }}" />
                {% endif %}
                {% if event.talk %}
                <talk url="{{ DEFAULT_URL_PREFIX }}{{ event.talk.get_absolute_url }}">
                    {% for spk in event.talk.get_all_speakers %}
                    <speaker url="{{ DEFAULT_URL_PREFIX }}{{ spk.get_absolute_url }}" name="{{ spk.name }}" image="{{ DEFAULT_URL_PREFIX }}{{ MEDIA_URL }}stuff/speaker/{{ spk.image|image_resized }}" />
                    {% endfor %}
                    {% for b in event.talk.abstracts.all %}
                    <abstract lang="{{ b.language }}">{{ b.body|striptags }}</abstract>
                    {% endfor %}
                </talk>
                {% endif %}
                </event>
                {% endif %}
            {% endfor %}
        </slot>
        {% endif %}
    {% endfor %}
</schedule>
{% endspaceless %}
